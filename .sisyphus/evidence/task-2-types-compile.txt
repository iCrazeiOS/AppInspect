Task 2: Shared TypeScript Types — Compilation Evidence
=======================================================

Command:
  npx tsc --noEmit --strict --target ES2022 --moduleResolution bundler --ignoreConfig src/shared/types.ts src/shared/ipc-types.ts

Result: SUCCESS (exit code 0, no output — clean compilation)

Files created:
  - src/shared/types.ts
  - src/shared/ipc-types.ts

Notes:
  - Used --moduleResolution bundler (node10 is deprecated in TS 7.x)
  - Used --ignoreConfig since tsconfig.json already exists from another task
  - No `any` or `unknown` used; PlistValue recursive type replaces `any` for plist values
  - bigint used for 64-bit Mach-O addresses and sizes (Section.addr, Section.size, Segment.vmaddr, Segment.vmsize, Symbol.address)
  - LoadCommand uses discriminated union on `type` field with 6 variants
  - TabData uses discriminated union on `tab` field with 10 variants
